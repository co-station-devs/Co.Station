coverage:
  status:
    project:
      default: off
      frontend:
        # target: 50%
        flags: frontend
      backend:
        # target: 50%
        flags: backend

flags:
  backend:
    paths:
      - server/
  frontend:
    paths:
      - src/
